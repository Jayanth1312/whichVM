Stop the development servers:

1. Find and kill the process running on port 8080 (backend) using `lsof -ti:8080`
2. Find and kill the process running on port 3000 (frontend) using `lsof -ti:3000`
3. Confirm both ports are free
