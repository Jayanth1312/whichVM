Start the development servers:

1. Run `npm install --prefix backend` and `npm install --prefix frontend` to install dependencies
2. Run `npm run dev --prefix backend` in the background
3. Run `npm run dev --prefix frontend` in the background
4. Verify backend is running by curling http://localhost:8080/api/ping
5. Confirm frontend is running on http://localhost:3000
